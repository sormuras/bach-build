name: Test

on: push

jobs:

  test:
    name: 'Java ${{ matrix.java }} on ${{ matrix.os }}'
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest , macos-latest, windows-latest ]
        java: [ 16-ea ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - uses: sormuras/bach-build@main
        with:
          bach-version: main
          bach-action: build
          project-name: Test
          project-version: 47.11-${{ github.sha }}
          project-main-release: 9
          project-main-jarslug: slug
          working-directory: .
      - run: jar --describe-module --file .bach/workspace/modules/test@slug.jar
